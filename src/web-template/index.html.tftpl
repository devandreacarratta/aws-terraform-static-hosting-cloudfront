<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>${shop_name} - Local Shop</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <header>
        <nav class="navbar">
            <div class="nav-brand">
                <h1>${shop_name}</h1>
            </div>
            <ul class="nav-menu">
                <li><a href="index.html" class="active">Home</a></li>
                <li><a href="products.html">Products</a></li>
                <li><a href="products-adv.html">Advanced Products</a></li>
                <li><a href="product-add.html">Add Product</a></li>
            </ul>
        </nav>
    </header>

    <main>
        <section class="hero">
            <div class="hero-content">
                <h2>${welcome_message}</h2>
                <p>${shop_description}</p>
                <div class="hero-buttons">
                    <a href="products.html" class="btn btn-primary">${view_products}</a>
                    <a href="#contact" class="btn btn-secondary">${contact_us}</a>
                </div>
            </div>
        </section>

        <section class="features">
            <div class="container">
                <div class="feature-grid">
                    <div class="feature-card">
                        <h3>${feature_1_title}</h3>
                        <p>${feature_1_desc}</p>
                    </div>
                    <div class="feature-card">
                        <h3>${feature_2_title}</h3>
                        <p>${feature_2_desc}</p>
                    </div>
                    <div class="feature-card">
                        <h3>${feature_3_title}</h3>
                        <p>${feature_3_desc}</p>
                    </div>
                </div>
            </div>
        </section>

        <section id="contact" class="contact">
            <div class="container">
                <h2>${contact_title}</h2>
                <div class="contact-info">
                    <p><strong>${address_label}:</strong> ${shop_address}</p>
                    <p><strong>${phone_label}:</strong> ${shop_phone}</p>
                    <p><strong>${email_label}:</strong> ${shop_email}</p>
                    <p><strong>${hours_label}:</strong> ${shop_hours}</p>
                </div>
            </div>
        </section>
    </main>

    <footer>
        <div class="container">
            <p>&copy; ${current_year} ${shop_name}. ${rights_reserved}</p>
            <p class="kiro-credit">Generated by <a href="https://kiro.dev" target="_blank" rel="noopener">kiro.dev</a></p>
        </div>
    </footer>
</body>
</html>